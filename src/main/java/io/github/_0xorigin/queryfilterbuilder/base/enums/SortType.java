package io.github._0xorigin.queryfilterbuilder.base.enums;

/**
 * Specifies the type of sort being applied.
 */
public enum SortType {
    /**
     * A standard sort that maps directly to an entity field.
     */
    NORMAL,
    /**
     * A custom sort that uses a predefined, complex logic.
     */
    CUSTOM
}
