package io.github._0xorigin.queryfilterbuilder.base.enums;

/**
 * Specifies the type of filter being applied.
 */
public enum FilterType {
    /**
     * A standard filter that maps directly to an entity field.
     */
    NORMAL,
    /**
     * A custom filter that uses a predefined, complex logic.
     */
    CUSTOM
}
